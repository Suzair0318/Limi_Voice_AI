# --- Web framework / ASGI ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
python-dotenv==1.0.1

# --- Settings / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- LLM / Agent orchestration ---
langchain==0.3.14
langchain-core==0.3.29
langchain-openai==0.2.14
langgraph==0.2.62
openai==1.59.6

# --- Audio processing ---
pydub==0.25.1
# NOTE: pydub requires ffmpeg installed on the host system (apt-get install ffmpeg / brew install ffmpeg)

# --- Test client only (mic capture + speaker playback) ---
sounddevice==0.5.5
numpy==2.5.0
