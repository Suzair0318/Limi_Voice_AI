# --- Web framework / ASGI ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
python-dotenv==1.0.1

# --- Settings / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Audio processing ---
# NumPy powers the in-process PCM resampling between the device (16k/48k) and
# the OpenAI Realtime API (24k).
numpy==2.5.0

# --- Test client only (mic capture + speaker playback + file decode) ---
# pydub requires ffmpeg on the host only when decoding non-WAV files.
pydub==0.25.1
sounddevice==0.5.5
